# دليل النشر الحالي — آخر تحديث 2026-09-16

> **اقرأ هذا الملف قبل أي رفع أو تشغيل SQL.** كل ما هو معلَّق في ترتيب واحد آمن.
> القاعدة الذهبية: **عارض الأسعار أولاً، ثم قاعدة البيانات، ثم ملفات المنظومة.**
> عكس الترتيب (تطبيق 0053 قبل رفع العارض الجديد) يُعمي عارض الأسعار في المحل فوراً.

---

## المرحلة ١ — عارض الأسعار (إلزامية أولاً)

**ارفع الملفين** (معاً) من `updated-html-files/pricechecker/` إلى مكان عارض الأسعار:

```
index.html
sw.js
```

**تحقق (على التحميل الثاني للصفحة — لا الأول):**
1. البصمة `b20260916-1339` تظهر في رقاقة الشريط العلوي وفي بطاقة الدخول
2. سجّل الدخول على تابلت المحل ⇒ ابحث عن صنف ⇒ يظهر **الاسم والسعر والكمية في كل فرع واسم الفرع**
3. لو فشل شيء: أرسل لي ما تراه حرفياً ولا تكمل للمرحلة ٢

---

## المرحلة ٢ — قاعدة البيانات (بعد نجاح المرحلة ١ فقط)

**أ) لقطة قبلية لـ 0054** — شغّل في SQL Editor الاستعلام الموجود في رأس الملف
`supabase/migrations/0054_created_by_backfill.sql` (يبدأ بـ `with cand as`).
احفظ النتيجة — هي «كم فاتورة ستُعبّأ وكم ستبقى NULL ولماذا».

**ب) التطبيق** — واحدة من اثنتين:
- `supabase db push` (يطبّق كل ما لم يُطبّق بالترتيب: 0049…0055 — كلها آمنة لإعادة التشغيل)، أو
- الملفات يدوياً واحداً واحداً في SQL Editor من `updated-html-files/benamor-sales-system/` بالترتيب:
  `supabase-pos-suppliers-dedup.sql` ← `supabase-pos-location-category-rules.sql` ← `supabase-pos-stock-requests.sql` ← `supabase-pos-suggestion-dismissals.sql` ← `supabase-pos-anon-lockdown.sql` ← `supabase-pos-created-by-backfill.sql` ← `supabase-pos-return-without-invoice.sql`

كل ملف بيان واحد (كتلة DO) لا يقسّمه SQL Editor.

**ج) التحقق بعدها:**
1. شغّل `supabase/verify-0049-0052.sql` كاملاً — يجب: 90 مورداً · 4445 منتجاً بمورد · 471 صفاً · عمودا hit_count وlast_requested_at موجودان · anon لا شيء · الدوال STABLE
2. سكربت فحص anon من المستشار (console أي متصفح على صفحة المنظومة): كل جدول `pos_` يعطي 401 أو صفر صف، و`login_app_user` يعطي 401/403 لا 400
3. **أرسل لي النتائج** (لقطة أو نص) — خاصة لو 0050 ≠ 471 بالضبط: توقّف وأخبرني قبل أي شيء آخر

---

## المرحلة ٣ — ملفات المنظومة (POS)

**ارفع الأربعة** من `updated-html-files/benamor-sales-system/`:

```
index.html   app.js   app.css   sw.js
```

هذه البصمة `b20260916-1339` تجمع: تنظيم شاشة البيع الجديدة + إصلاحات الجهاز الثلاثة
(بحث الزبائن، زر الدفع دائم الظهور، وضع التعديل) + تلوين أسطر الهامش + مرتجع بلا فاتورة
+ قائمة جرد مطلوب. **لا تستعمل زر «مرتجع بدون فاتورة» قبل تطبيق 0055** (المرحلة ٢).

**تحقق على التحميل الثاني** — البصمة في الإعدادات: `20260916-1339`، ثم:
- [ ] بحث زبون: شريحة الزبون ⇒ تكتب اسماً/هاتفاً ⇒ القائمة تُصفّى، وEnter لا يفعل شيئاً تخريبياً
- [ ] زر الدفع مثبَّت أسفل الشاشة دائماً — مع أصناف كثيرة وفي وضع التعديل
- [ ] صنف هامشه 45% بلا لون؛ اجعل خصم سطره 40% ⇒ يحمرّ فوراً
- [ ] 10 أصناف عادية ⇒ الشاشة بلا ألوان تقريباً
- [ ] فاتورة بيع بثلاثة أصناف + زبون + ملاحظة + سعر جملة ⇒ تُحفظ كل القيم كما قبل
- [ ] «مرتجع بدون فاتورة» بمئة د.ل نقداً ⇒ المخزون +1 والخزينة −100 والسبب إلزامي
- [ ] التحويلات ⇒ «جرد مطلوب» ⇒ العدد الفعلي (المتوقع ≈ 222 صفاً / 215 صنفاً)

---

## المرحلة ٤ — الموقع العام

- اطلب طلباً تجريبياً من الموقع ⇒ **يُحفظ بنجاح** (إدراج anon بقى كما هو)
- صفحات المنتجات تفتح وتقرأ كالعادة

---

## أرقام بانتظارها منك (أرسلها متى توفرت)

| ماذا | المتوقع | أين يظهر |
|---|---|---|
| مورّدون بعد تنظيف 0049 | 90 | verify-0049-0052.sql |
| صفوف قواعد الأقسام 0050 | **471 بالضبط** (ولو غير ذلك توقّف وأخبرني) | verify-0049-0052.sql |
| كم ستُعبّأ 0054 من created_by | لقطة ما قبلية | استعلام رأس 0054 |
| جرد مطلوب | ≈222 صفاً / 215 صنفاً | تبويب جرد مطلوب |
| سطر السياق في اقتراحات التحويل | يظهر عند اختيار صنف | شاشة البيع |

---

## ممنوعات دائمة

- لا SQL من المتصفّح — كل تغيير قاعدة = migration مرقّم
- لا مفتاح service_role في أي ملف يُرفع
- تحقّق دائماً على **تحميل ثانٍ** (الـSW) وقارن البصمة في الإعدادات
- عند أي خطأ: أرسل لي الرسالة **حرفياً** مع رقم البصمة — لا تعِد المحاولة عشوائياً
